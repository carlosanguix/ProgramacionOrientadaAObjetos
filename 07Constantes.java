package ejercicio07;

import java.util.ArrayList;
import java.util.Arrays;

public class Constantes {

	// Esta clase podía no haber sido creada, pero para tenerlo todo más organizado
	// es recomendable crear clases que tengan una única función definida.
	public static final ArrayList<Integer> valores = new ArrayList<>(Arrays.asList(1, 2, 5, 10, 20, 50, 100, 200));
	public static final ArrayList<String> lados = new ArrayList<String>(Arrays.asList("cara", "cruz"));
}
